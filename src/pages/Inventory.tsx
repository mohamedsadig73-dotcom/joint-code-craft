/**
 * Legacy thin re-export. The real implementation now lives in
 * `src/modules/inventory/InventoryModule.tsx` (Sprint 4 / P3).
 * Kept until S7 cleanup so deep imports keep working.
 */
export { default } from '@/modules/inventory/InventoryModule';
